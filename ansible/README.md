# Ansible

Configuration examples and baseline automation patterns for Linux hosts.

## Roles

- **roles/baselines/**  
  Template role for applying baseline configuration to Linux hosts. Demonstrates role structure, idempotent task patterns, and reusable defaults for building standardized roles.

## Playbooks

- **playbooks/baseline.yml**  
  Example playbook showing how to apply the baseline role to a group of Linux hosts.
